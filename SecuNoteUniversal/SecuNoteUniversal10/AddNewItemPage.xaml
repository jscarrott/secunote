<Page
    x:Class="SecuNoteUniversal10.AddNewItemPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SecuNoteUniversal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock x:Name="txtBlockHeader" Text="SecuNote" FontSize="48" FontWeight="Bold" />
        <RelativePanel RelativePanel.Below="txtBlockHeader">
            <Button x:Name="SelectFileButton" Content="Select File" Click="SelectFileButton_Click" />
            <Button x:Name="ConfirmButton" RelativePanel.Below="SelectFileButton" Content="Confirm"
                    Click="Button_Click" />
            <ComboBox x:Name="TypeBox" RelativePanel.RightOf="SelectFileButton" />
        </RelativePanel>

    </RelativePanel>
</Page>